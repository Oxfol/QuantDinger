name: Temporary V6b shrinkage runner

on:
  pull_request:
    branches: [main]
    paths:
      - tmp-v3-data/v4_temporal_optimizer.py
      - tmp-v3-data/v6_shrinkage_driver.py
      - .tmp-v6b-trigger

permissions:
  contents: write

jobs:
  optimize:
    if: github.head_ref == 'tmp-dog-bot-v3-research'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - run: python -m pip install --disable-pip-version-check -q numpy pandas scikit-learn
      - name: Download corpus
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/v6b tmp-v3-data/v6b-results
          curl -L --fail --retry 7 --retry-all-errors --connect-timeout 30 --max-time 600 -A 'dog-bot-v6b-research/1.0' 'https://zenodo.org/records/21149176/files/rejection_outcomes.jsonl.gz?download=1' -o /tmp/v6b/rejection_outcomes.jsonl.gz
          echo '38f7ffeb8a2d55a5b05e7d304cab11e5  /tmp/v6b/rejection_outcomes.jsonl.gz' | md5sum -c -
      - name: Optimize
        id: optimize
        continue-on-error: true
        shell: bash
        run: PYTHONPATH=tmp-v3-data python tmp-v3-data/v6_shrinkage_driver.py --corpus /tmp/v6b/rejection_outcomes.jsonl.gz --output tmp-v3-data/v6b-results > tmp-v3-data/v6b-results/run.log 2>&1
      - name: Preserve results
        if: always()
        shell: bash
        run: |
          cp tmp-v3-data/v6b-results/run.log tmp-v3-data/v6b-results/run-output.txt 2>/dev/null || true
          printf 'outcome=%s\nrun_id=%s\n' '${{ steps.optimize.outcome }}' '${{ github.run_id }}' > tmp-v3-data/v6b-results/workflow-status.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f tmp-v3-data/v6b-results
          if ! git diff --cached --quiet; then git commit -m 'research: add V6b shrinkage output'; git push origin HEAD:tmp-dog-bot-v3-research; fi
      - if: steps.optimize.outcome != 'success'
        run: exit 1
